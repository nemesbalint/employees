spring:
  config:
    activate:
      on-profile: prod
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: create

  datasource:
    url: jdbc:mariadb://localhost:3307/employees
    username: employees
    password: employees

logging:
    level:
      employees: debug
message: Hello (from application.yaml)